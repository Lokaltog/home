# Disable bell
xset b 0 &

# Set keyboard repeat rate
xset r rate 250 40 &

# Set font paths
for FONTPATH in `find $HOME/.fonts/bitmap/* -type d`; do
	[[ -d $FONTPATH ]] && xset +fp $FONTPATH 
done

xset fp rehash
